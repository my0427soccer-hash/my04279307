# App Store に出すまでの手順

このリポジトリには、ウェブ版をそのまま iOS アプリとして包んだ Xcode プロジェクトが
入っています（`native/ios/`）。Capacitor という仕組みを使っています。

**ただし、ここから先は Mac が必須です。** iOS アプリのビルド・署名・提出は
Apple の道具（Xcode）でしか行えず、Windows や Linux ではできません。

---

## 先に知っておいてほしいこと

お金と時間を使う前に、正直なところを3点書いておきます。

### 1. 費用と道具

| 必要なもの | 費用 |
| --- | --- |
| Mac（Xcode が動くもの） | — |
| Apple Developer Program | **年 12,980円**（99 USD 相当） |
| 審査 | 通常1〜3日、初回は長引くことがある |

年会費は、更新しないとアプリがストアから消えます。

### 2. 「ウェブサイトを包んだだけ」で落ちる可能性がある

Apple のガイドライン **4.2（最低限の機能）** は、ウェブサイトを WebView で
包んだだけのアプリを繰り返し拒否しています。このアプリは
「オフラインで動く」「データは端末内に保存される」「単なる公開サイトの表示ではない」
という点で反論できますが、**一度で通る保証はありません。**

### 3. 「実在の人物を評価する」という中身そのものが論点になりうる

これがいちばん大きな懸念です。このアプリは、実名で登録した人を
「一番かっこいい人」「一番モテる人」などで順位づけします。

Apple はガイドライン **1.2（ユーザー生成コンテンツ）** で、特定の個人を
標的にできる仕組みに厳しく、実在の人物を評価・採点するアプリは過去に
拒否や削除の対象になっています。**通らない可能性は現実にあります。**

反論の材料になるのは次の点です。審査メモに書いてください。

- 参加者は自分たちで名簿を作る**閉じたグループ専用**で、公開の場がない
- 誰でも検索・閲覧できる公開プロフィールや投稿フィードが存在しない
- 匿名で第三者にメッセージを送る機能がない
- データは端末内（と、設定した場合は主催者自身のスプレッドシート）にしか出ない

それでも落ちる場合、質問文から容姿・恋愛に関する項目を外すのが現実的な妥協案です。

**先に PWA（ホーム画面に追加）で運用してみて、本当にストアが必要か
確かめてからのほうが安全です。** 使い勝手はほぼ同じです。

---

## 手順

### 0. 準備（Mac で）

1. App Store から **Xcode** を入れる
2. [developer.apple.com](https://developer.apple.com/programs/) で
   **Apple Developer Program** に登録する（年会費）
3. Node.js を入れる（[nodejs.org](https://nodejs.org/)）

### 1. プロジェクトを開く

```bash
git clone https://github.com/my0427soccer-hash/my04279307.git
cd my04279307/native
npm install
npm run sync        # ウェブ版を取り込んで iOS 側に反映
npx cap open ios    # Xcode が開く
```

`index.html` を編集したときは、そのつど `npm run sync` を実行してください。

### 2. Xcode で設定する

左の **App** → **Signing & Capabilities**:

- **Team**: 自分の Apple Developer アカウントを選ぶ
- **Bundle Identifier**: `com.my0427soccer.hyokaradar`
  （他の人と重複しない文字列に変えても構いません。変える場合は
  `native/capacitor.config.json` の `appId` も同じ値にしてください）
- **Automatically manage signing** にチェック

**General** タブ:

- **Display Name**: 相互評価レーダー
- **Version**: 1.0.0 / **Build**: 1

実機を繋いで ▶ を押すと、自分の iPhone で動きます。まずここまで確認してください。

### 3. App Store Connect に登録する

[appstoreconnect.apple.com](https://appstoreconnect.apple.com/) →
マイApp → ＋ → 新規App

| 項目 | 内容 |
| --- | --- |
| プラットフォーム | iOS |
| 名前 | 相互評価レーダー |
| プライマリ言語 | 日本語 |
| バンドルID | 手順2で設定したもの |
| SKU | 何でもよい（例 `hyoka-radar-001`） |

### 4. 提出に必要なもの

- **アイコン** 1024×1024（`icons/appstore-1024.png` を使ってください。
  透過なしの RGB で作ってあるので、そのまま通ります）
- **スクリーンショット** 6.7インチ（iPhone 15 Pro Max など）で最低3枚。
  Xcode のシミュレータで撮れます
- **プライバシーポリシーのURL**（必須）。`PRIVACY.md` を用意してあるので、
  GitHub Pages で公開してそのURLを使ってください
- **サポートURL**（必須）。リポジトリのURLで構いません
- **年齢制限**: 「まれ/軽度の成人向けまたはきわどいテーマ」を選んでおくのが無難
- **App のプライバシー**: 「データを収集していません」で申告できます
  （自動集計を設定した場合は、主催者自身のGoogleスプレッドシートに送信される旨を
  申告してください）

### 5. 審査メモに必ず書くこと

**ここを書かないと確実に落ちます。** 結果画面はパスワードで保護されていて、
しかも参加者全員が回答しないと開きません。審査員は一人なので、そのままでは
アプリの半分が確認できません。

審査メモ（App Review Information → Notes）にこう書いてください:

```
このアプリは、少人数の閉じたグループが互いを順位づけし、
結果をレーダーチャートで見るためのものです。
公開の投稿機能やSNS機能はなく、データは端末内に保存されます。

【結果画面の確認方法】
1. 「回答する」タブで参加者名を3人以上入力し「この設定で始める」
2. 「結果を見る」タブを開き、パスワード My04279307 を入力
3. 「サンプルデータで結果画面を見る」を押すと、
   架空のデータで結果画面全体を確認できます

結果パスワード: My04279307
```

手順3の「サンプルデータ」は、まさにこの確認のために用意した機能です。

### 6. アップロード

Xcode で **Product → Archive** →
**Distribute App** → **App Store Connect** → **Upload**

App Store Connect でビルドを選び、審査に提出します。

---

## 落ちたときは

拒否の通知には理由のガイドライン番号が書かれています。

| 番号 | 意味 | 対応 |
| --- | --- | --- |
| 2.1 | 動作を確認できない | 審査メモの手順（上記）が伝わっていない。返信で補足する |
| 4.2 | 機能が最低限すぎる | オフライン動作と端末内保存を説明する。通らなければ機能追加が必要 |
| 1.2 | ユーザー生成コンテンツ | 閉じたグループ専用であることを説明。それでも駄目なら質問内容の見直し |

Resolution Center から返信して議論できます。1回で通らなくても普通のことです。

---

## Android（Google Play）も出す場合

`npx cap add android` で Android プロジェクトも作れます。Play は登録料が
初回25ドルのみ、審査も比較的緩やかです。Mac も不要なので、こちらのほうが
はるかに簡単です。必要なら言ってください。

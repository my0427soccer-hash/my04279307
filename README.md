# 相互評価レーダー

5人（山下・服部・大矢・福井・内田）が互いを他人評価し、結果をレーダーチャートと
ランキングで見るためのツールです。`index.html` 1ファイルだけで動きます。
サーバーもビルドも不要で、ダブルクリックで開けます。

## 使い方

### 1. 回答する

1. `index.html` を開き、「回答する」タブで自分の名前を選ぶ
2. 質問ごとに、自分以外の4人を上位からタップして1位〜4位に並べる
   （3人選ぶと残り1人は自動で4位になります）
3. 全60問おわったら「回答を確定してコードを作る」
4. 表示された **66文字の回答コード** を集計担当に送る

回答は入力中も送信後もこの端末のブラウザにだけ保存され、他の人の回答は一切表示されません。

### 2. 集計する

「結果を見る」タブでパスワード **`My04279307`** を入力し、5人分の回答コードを
貼り付けて「読み込む」。改行で区切れば何件でもまとめて読み込めます。

5人全員が同じ端末で回答した場合は、コードを貼り付けなくてもそのまま集計されます。

### 3. 結果を見る

| セクション | 内容 |
| --- | --- |
| 総合ランキング | 全60問の平均pt順 |
| カテゴリ別レーダー | 1人1枚、10カテゴリの10角形レーダー |
| 重ねて比べる | 選んだ人を1枚に重ねる（最大3人） |
| カテゴリ別ランキング | 10カテゴリ × 5人の平均ptと順位 |
| 質問別ランキング | 全60問それぞれの1位〜5位 |
| 全データ | 全数値の表と、表計算ソフトへの貼り付け用テキスト |

## 採点方法

各質問で、評価する側は自分以外の4人に順位をつけます。

| 順位 | 1位 | 2位 | 3位 | 4位 |
| --- | --- | --- | --- | --- |
| 得点 | 4pt | 2pt | 1pt | 0pt |

自分自身は評価しないので、1人あたり4人から評価を受けます。
1問の満点は 4pt × 4人 = 16pt です。

表示している「平均pt」は **評価者1人あたりの得点（0〜4pt）** です。
評価者が4人そろっていなくても比較できるようにこの形にしています。

**1.75pt が基準値** です。各評価者は 4 + 2 + 1 + 0 = 7pt を4人に配るので、
1人あたりの平均は必ず 1.75pt になります。レーダーチャートの中央にある薄いゾーンが
この基準の内側で、そこから外にはみ出しているところがその人の強みです。

質問は10カテゴリ × 6問の計60問。カテゴリは
能力・知性／外見・魅力／性格・人間性／コミュニケーション／仕事・お金／メンタル／
身体能力／恋愛・人間関係／クリエイティブ／その他 です。

## 回答コードについて

60問ぶんの回答を66文字に詰めたものです。1問の順位は4人の並べ方24通りなので、
1文字（`I` と `O` を除いた24文字）でちょうど表せます。

```
PR1 0 AAAAAAAA...AAAA TQ
│   │ │                └─ チェックサム2文字（写し間違いの検出用）
│   │ └─ 60問ぶんの回答（1問=1文字）
│   └─ 回答者（0=山下, 1=服部, 2=大矢, 3=福井, 4=内田）
└─ 形式の識別子
```

読み込むときは大文字・小文字とスペースを区別しません。表示のスペースは
読みやすさのためだけのものなので、含めても含めなくても構いません。

## 注意点

- **パスワードは覗き見防止です。** ブラウザだけで動くツールなので、ページのソースを
  見ればパスワードは読めますし、回答コードも解読できます。回答した人が結果を先に
  見てしまうのを防ぐためのもので、本気で秘密を守る仕組みではありません。
- **データは端末のブラウザにだけ残ります。** 別の端末とは共有されません。
  シークレットウィンドウや、ブラウザのデータ消去には注意してください。
- 5人・順位式という前提で作ってあります。人数や配点を変える場合は `index.html` の
  `PEOPLE` / `PTS` / `CATS` を書き換えてください（コードの形式も変わります）。

## みんなに配る

- **一番かんたん**：`index.html` をそのまま送る（メール添付・チャット等）。
  各自が開いて回答し、出てきたコードを返す。
- **リンクで配る**：GitHub Pages を有効にすると
  `https://<ユーザー名>.github.io/<リポジトリ名>/` で開けます。
  リポジトリの Settings → Pages → Source をこのブランチの `/ (root)` に設定してください。

## 設計メモ

- 依存ライブラリなし。チャートは素の SVG で描画しています。
- 配色は8色のカテゴリカルパレットの先頭3スロットを使っています。1枚に重ねられるのを
  3人までにしているのは、4色以上を重ねると P型・D型色覚で線が判別できなくなるためです。
  重ねるときは色に加えてマーカーの形（●■◆）でも区別できるようにし、同じ数値を必ず
  表でも出しています。
- ライト／ダーク両方に対応（OS設定と、閲覧側のテーマ切り替えの両方に追従）。
